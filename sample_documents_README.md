# Local sample documents

For privacy, project PDFs should not be committed. Create a `sample_documents` folder and
place local test PDFs there. The repository's `.gitignore` excludes PDFs in that folder.
